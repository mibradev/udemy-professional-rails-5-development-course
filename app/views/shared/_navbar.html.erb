<% navbar_items.each do |item| %>
  <% navbar_link = link_to(
    item[:name],
    item[:path],
    method: item[:method],
    class: [html_class, ('active' if current_page? item[:path])]
  ) %>

  <%= local_assigns[:html_tag] ? content_tag(html_tag, navbar_link) : navbar_link %>
<% end %>
