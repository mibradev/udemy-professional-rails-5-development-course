<main role="main" class="container mt-5 mb-5">
  <h1 class="mb-4"><%= @topic.title %></h1>

  <% if @blogs.present? %>
    <%= render @blogs %>
    <%= paginate @blogs %>
  <% else %>
    <p>No posts found.</p>
  <% end %>
</main>
